{
  "name": "brawl-counters-tests",
  "private": true,
  "type": "module",
  "description": "Dev-only test harness for docs/calculate.js. Not deployed (GitHub Pages serves docs/ only).",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "generate": "node scripts/generate-counter-pages.mjs",
    "stamp": "node scripts/stamp-version.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
